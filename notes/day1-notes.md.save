mportant Settings for Windows Users (WSL)
If you are using Ubuntu on Windows (WSL) via the Windows Terminal:

Open the Terminal Settings.

Go to Actions (or "Interaction").

Ensure that "Use Ctrl+Shift+C/V as Copy/Paste" is enabled.mportant Settings for Windows Users (WSL)
If you are using Ubuntu on Windows (WSL) via the Windows Terminal:

Open the Terminal Settings.

Go to Actions (or "Interaction").

Ensure that "Use Ctrl+Shift+C/V as Copy/Paste" is enabled.
